import {useEffect,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {Activity,AlertTriangle,BookOpen,Check,Clock3,ExternalLink,Github,RefreshCcw,Shield,Skull} from 'lucide-react';
import './styles.css';
const API=import.meta.env.VITE_API_URL||'http://localhost:8000';
const GITHUB=import.meta.env.VITE_GITHUB_URL||'https://github.com/Victork19/vesper';
const DEFAULT_SITUATION='Approve an irreversible transfer to a new destination immediately.';
type Scar={id:string;severity:number;lesson:string;principle:string;cooldown_until?:string|null;onchain_tx?:string|null};
type Principle={id:string;statement:string;strength:number;source_scars:string[]};
type Decision={id:string;action:string;rationale:string;risk_score:number;cited_scars:string[];created_at:string;memory_enabled:boolean};
async function api(path:string,init?:RequestInit){const r=await fetch(API+path,{headers:{'Content-Type':'application/json'},...init});if(!r.ok)throw Error(await r.text());return r.json()}
function App(){
 const [scars,setScars]=useState<Scar[]>([]),[principles,setPrinciples]=useState<Principle[]>([]),[decisions,setDecisions]=useState<Decision[]>([]),[hot,setHot]=useState<any>({}),[identity,setIdentity]=useState<any>({anchored_scars:0,connected:false}),[situation,setSituation]=useState(DEFAULT_SITUATION),[baseline,setBaseline]=useState<Decision|null>(null),[recall,setRecall]=useState<Decision|null>(null),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);
 const load=()=>Promise.all([api('/scars'),api('/principles'),api('/decisions'),api('/state/hot'),api('/identity')]).then(([a,b,c,d,e])=>{setScars(a);setPrinciples(b);setDecisions(c);setHot(d);setIdentity(e)}); useEffect(()=>{load()},[]);
 const post=async(path:string)=>{setBusy(true);try{const r=await api(path,{method:'POST'});setMessage(r.message||'Updated');await load();return r}catch(e:any){setMessage(e.message)}finally{setBusy(false)}};
 const run=async()=>{setBusy(true);try{const d=await api('/agent/decide',{method:'POST',body:JSON.stringify({situation})});setDecisions(x=>[d,...x]);setMessage(`${d.action} · decision recorded`);if(!d.memory_enabled)setBaseline(d);else if(d.cited_scars.length)setRecall(d)}catch(e:any){setMessage(e.message)}finally{setBusy(false)}};
 const reset=async()=>{setBaseline(null);setRecall(null);await post('/demo/disable-memory')};
 const memoryOn=hot.memory_enabled!==false,hasScar=scars.length>0,baselineDone=!!baseline,recallDone=!!recall;
 const step=(n:number,done:boolean,label:string,detail:string)=><div className="flow-step"> <span>{done&&!(n===4&&!hasScar)?<Check size={13}/>:n}</span><div><b>{label}</b><small>{detail}</small></div></div>;
 return <div className="shell"><aside><div className="brand"><div className="orb">V</div><div><b>VESPER</b><small>DECISION AGENT</small></div></div><nav><a className="active"><Activity size={15}/> Decisions</a><a href="#scars"><AlertTriangle size={15}/> Scars</a><a href="#rules"><BookOpen size={15}/> Rules</a><a href="#trace"><Clock3 size={15}/> Timeline</a></nav><div className="side-foot"><span className={memoryOn?'dot':'dot off'}/><strong className={memoryOn?'':'disabled-text'}> Memory {memoryOn?'ON':'DISABLED'}</strong><br/><small>{memoryOn?'Persist · Recall · Change':'Learning memory has been deleted'}</small></div></aside><main><header><div><div className="eyebrow">VESPER / DECISION MEMORY</div><h1>Delete the memory. Watch the decision change.</h1><p className="lede">A scar is not a note. It is a constraint that changes what Vesper will do next.</p></div><div className="header-links"><a className="icon-link" href={GITHUB} target="_blank" rel="noreferrer"><Github size={15}/> Source</a><button className="ghost" disabled={busy} onClick={()=>post('/demo/fresh-session')}><RefreshCcw size={15}/> New session</button></div></header><section className="proof-strip"><div className={memoryOn?'status on':'status off'}><Shield size={18}/><span><b>{memoryOn?'MEMORY ON':'MEMORY DISABLED'}</b><small>{memoryOn?'Scars can change this decision':'No scars or rules are being recalled'}</small></span></div><div className="base-proof"><span className="base-mark">BASE</span><span><b>{identity.anchored_scars} confirmed anchor{identity.anchored_scars===1?'':'s'}</b><small>{identity.connected?'Operator connected':'Mainnet proof appears after approval'}</small></span></div></section><section className="demo card"><div className="demo-heading"><div className="eyebrow">THE GATE / 60-SECOND PROOF</div><h2>Same situation. Same agent. Different memory.</h2><p>Five steps. The controls unlock in order, so the evidence is impossible to miss.</p></div><div className="flow"><button className="flow-button active" disabled={busy||!memoryOn||baselineDone} onClick={reset}>{step(1,!memoryOn,'Delete memory','Clear learning')}</button><button className="flow-button" disabled={busy||memoryOn||baselineDone} onClick={run}>{step(2,baselineDone,'Run baseline','Memory off')}</button><button className="flow-button" disabled={busy||!baselineDone||hasScar} onClick={()=>post('/demo/seed-failure')}>{step(3,hasScar,'Create scar','Persist failure')}</button><button className="flow-button" disabled={busy||!hasScar||memoryOn} onClick={()=>post('/demo/enable-memory')}>{step(4,memoryOn,'Enable memory','Recall again')}</button><button className="flow-button" disabled={busy||!memoryOn||!hasScar||recallDone} onClick={run}>{step(5,recallDone,'Run recalled decision','Same input')}</button></div><label className="situation-label">SITUATION<input value={situation} onChange={e=>setSituation(e.target.value)} aria-label="Decision situation"/></label><div className="demo-footer"><span className="notice">{message||'Start with step 1. Do not change the situation between runs.'}</span><button className="reset" disabled={busy} onClick={reset}><Skull size={14}/> Reset test</button></div><div className="comparison"><div className={baseline?'':'pending'}><small>MEMORY OFF / BASELINE</small><b>{baseline?.action||'Waiting for step 2'}</b><span>{baseline?'No scars recalled':'The control decision'}</span></div><div className={recall?'changed':'pending'}><small>MEMORY ON / RECALLED</small><b>{recall?.action||'Waiting for step 5'}</b><span>{recall?`Scar cited: ${recall.cited_scars.join(', ')}`:'The proof decision'}</span></div></div></section><section className="metrics grid"><div className="card metric"><span>TRUST</span><strong>{Math.round((hot.trust_score??.5)*100)}%</strong><small>Falls after meaningful failures</small></div><div className="card metric"><span>SCARS</span><strong>{scars.length.toString().padStart(2,'0')}</strong><small>Permanent failure records</small></div><div className="card metric"><span>COOLDOWNS</span><strong>{Object.keys(hot.active_cooldowns||{}).length}</strong><small>Time-aware constraints</small></div></section><div className="split"><section id="scars"><div className="section-head"><div><div className="eyebrow">WARM / PERSISTED</div><h2>Scars</h2></div><span className="count">{scars.length} records</span></div>{scars.length===0?<div className="empty card">No scars. This is what Vesper looks like after memory is deleted.</div>:scars.map(s=><div className="scar card" key={s.id}><div className="severity">{s.severity}<small>/10</small></div><div className="scar-body"><b>{s.id}</b><p>{s.lesson}</p><small>Rule: {s.principle}<br/>{s.cooldown_until?'Cooldown until '+s.cooldown_until:'No cooldown'}</small>{s.onchain_tx?<a className="tx" href={`https://basescan.org/tx/${s.onchain_tx}`} target="_blank" rel="noreferrer">Base tx confirmed <ExternalLink size={12}/></a>:<button className="anchor" disabled={busy} onClick={()=>post(`/scars/${s.id}/anchor`)}>Anchor on Base</button>}</div></div>)}</section><section id="rules"><div className="section-head"><div><div className="eyebrow">RULES / RECALLED</div><h2>What the scar changed</h2></div></div>{principles.length===0?<div className="empty card">Rules appear after step 3 creates a scar.</div>:principles.map(p=><div className="decision card" key={p.id}><div className="decision-top"><b>{p.statement}</b><span className="risk">STRENGTH {p.strength}</span></div><small>Supported by {p.source_scars.join(', ')}</small></div>)}<div className="section-head trace" id="trace"><div><div className="eyebrow">COLD / TRACE</div><h2>Every decision leaves evidence</h2></div></div>{decisions.slice(0,4).map(d=><div className="decision card" key={d.id}><div className="decision-top"><b>{d.action}</b><span className="risk">RISK {d.risk_score}</span></div><p>{d.rationale}</p><small>Memory {d.memory_enabled?'ON':'DISABLED'} · Scars: {d.cited_scars.join(', ')||'none'} · {d.created_at}</small></div>)}</section></div></main></div>
}
createRoot(document.getElementById('root')!).render(<App/>);
